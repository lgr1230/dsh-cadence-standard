{
  "name": "dsh-cadence-standard",
  "version": "0.1.0",
  "private": true,
  "description": "Thinking-budget pacing preset for DeepSeek Harness (Cadence Standard v2.3.1)",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check preset/cadence-bootstrap.mjs && node --check preset/cadence-core.mjs && node --test"
  }
}
